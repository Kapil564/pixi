# pixi Assistant — Technical Architecture & System Context

Welcome to the internal technical documentation for **pixi**, a privacy-first, Siri-like voice assistant for Windows built with Electron, TypeScript, SQLite, and pluggable STT/LLM/TTS providers.

---

## 1. System Overview

pixi is designed to bridge the gap between cloud-based AI convenience and 100% offline, local-first privacy. It functions as a native Windows desktop assistant operating silently in the System Tray with frameless, transparent overlay windows.

### Core Philosophy
* **Zero Mandated Cloud Lock-in:** pixi operates out-of-the-box using local models (Ollama, local Whisper, and Windows SAPI5 / Piper TTS) without requiring any API keys.
* **Smart Provider Fallback Matrix:** If API keys for cloud providers (OpenAI, Gemini, Groq, Fish Audio, ElevenLabs) are supplied in `.env`, pixi uses them. If an API key is missing or fails, pixi automatically falls back to local or free engines without crashing.
* **Per-User Local Data Isolation:** Transcripts, reminders, to-dos, and long-term memory files stay strictly on disk inside `%APPDATA%\pixi\`. There is no central server, remote telemetry tracking, or cloud database synchronization.

---

## 2. Directory & Repository Structure

```
pixi-assistant/
├── .env                       # Local environment variables and API keys
├── .env.example               # Template environment configuration
├── drizzle.config.ts          # Drizzle ORM configuration for SQLite
├── electron-builder.yml       # Production packaging configuration (NSIS & Portable)
├── index.html                 # HTML container for renderer UI
├── package.json               # Dependencies, scripts, and build targets
├── tsup.config.ts             # Bundler configuration for main process & preload
├── assets/                    # System tray and application icons
├── docs/                      # Technical documentation
│   └── context.md             # Architecture & system context guide (this file)
├── drizzle/                   # SQL migration scripts generated by drizzle-kit
└── src/                       # Source code root
    ├── actions/               # Intent action handlers
    │   └── executor.ts        # Maps LLM JSON intents to local system actions (reminders, to-dos)
    ├── db/                    # Local SQLite database layer
    │   ├── actions.ts         # DB queries for reminders and to-do items
    │   ├── index.ts           # SQLite client instantiation using better-sqlite3 & Drizzle
    │   ├── migrate.ts         # Automatic database schema migration runner
    │   ├── schema.ts          # Drizzle ORM tables schema (sessions, messages, reminders, todos)
    │   └── session-store.ts   # Session management and message logging
    ├── main/                  # Electron main process
    │   ├── index.ts           # Window management, tray creation, IPC bridge, global hotkeys, power monitors
    │   └── preload.ts         # Electron contextBridge exposing safe APIs to renderer
    ├── memory/                # Context & Memory System
    │   ├── context-pipeline.ts # Assembles system prompts per turn (profile + memory + history)
    │   ├── fact-extractor.ts  # Async non-blocking LLM background fact extraction
    │   ├── markdown-memory.ts # File-based Markdown memory manager (`%APPDATA%\pixi\memory\`)
    │   └── rolling-summary.ts # Periodic conversation summarizer & Gzip session archiver
    ├── orchestrator/          # Node.js Orchestrator Service
    │   ├── index.ts           # Socket.io server pipeline, VAD handler, request cancellation & lock logic
    │   └── scheduler.ts        # Node-cron background reminder polling & notification trigger
    ├── providers/             # Pluggable AI Service Providers
    │   ├── llm.ts             # OpenAI, Gemini, Groq, & Ollama intent parsing implementations
    │   ├── llm-router.ts      # LLM provider fallback routing logic
    │   ├── ollama-manager.ts  # Ollama server health check & model downloader (`pullLocalModel`)
    │   ├── piper-manager.ts   # Piper TTS binary & voice model downloader
    │   ├── setup-manager.ts   # 3-step unified background setup sequence manager
    │   ├── stt.ts             # OpenAI, Groq, & local faster-whisper STT implementations
    │   ├── stt-router.ts      # STT provider fallback routing logic
    │   ├── tts.ts             # Windows SAPI5, Piper, Fish Audio, ElevenLabs, & Azure TTS
    │   ├── tts-router.ts      # TTS provider fallback routing logic
    │   └── whisper-manager.ts # Whisper CLI binary & model downloader
    ├── renderer/              # Frontend UI (React + Tailwind CSS)
    │   ├── index.tsx          # Main React application component, Web Audio VAD, & mode toggle
    │   └── components/
    │       ├── PixelBlobCharacter.tsx # Custom SVG pixel-art blob mascot with animated phase states
    │       ├── WakeOrb.tsx            # Transparent 100x100 floating orb container
    │       └── Windows11Widget.tsx    # Horizontal Catppuccin command widget & expandable result card
    └── shared/                # Cross-process shared utilities
        ├── config.ts          # Centralized configuration loader for `.env` and defaults
        ├── http-util.ts        # Shared HTTP request helper
        ├── paths.ts           # OS path resolver (`%APPDATA%\pixi\` & asset locations)
        ├── provider-logger.ts # Provider activity logging
        └── types.ts           # Shared TypeScript interfaces & types
```

---

## 3. Services & Process Architecture

pixi employs a decoupled two-tier desktop architecture connected via Socket.io IPC:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       ELECTRON MAIN PROCESS (Node.js)                       │
│  - System Tray Management & Context Menus                                   │
│  - Frameless Transparent BrowserWindow Creation                             │
│  - Global Hotkey Registration (Cmd/Ctrl + Shift + Space)                     │
│  - Windows PowerMonitor (Sleep / Resume / Screen Unlock detection)          │
│  - Autostart / Login Item Settings (`openAtLogin`)                           │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │ IPC Bridge (Preload ContextBridge)
┌─────────────────────────────────────▼───────────────────────────────────────┐
│                          RENDERER PROCESS (React)                           │
│  - Audio Recording & Web Audio API Processing (16kHz PCM Float32 → WAV)     │
│  - Continuous Voice Activity Detection (VAD) & Web Speech API / FFT         │
│  - React UI (Catppuccin Windows 11 Widget & Pixel Blob Mascot Orb)           │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      │ Socket.io IPC (Port 3000)
┌─────────────────────────────────────▼───────────────────────────────────────┐
│                      ORCHESTRATOR SERVICE (Node.js)                          │
│  - Request Concurrency & Pipeline Cancellation (`activeRequestId`)          │
│  - Context Assembly (SQLite Sessions + Markdown Memory + Fact Extractor)   │
│  - Action Executor (Creates Reminders, To-dos, System Actions)              │
│  - Pluggable Routers (STT, LLM, TTS)                                         │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. Models, Pluggable Providers & Fallback Matrix

pixi evaluates available environment variables at startup and dynamically routes requests through the best active provider.

### Speech-to-Text (STT) Router
1. **OpenAI Whisper API:** Selected if `STT_PROVIDER=openai` and `OPENAI_API_KEY` is present.
2. **Groq Whisper:** Selected if `STT_PROVIDER=groq` and `GROQ_API_KEY` is present.
3. **Local faster-whisper (Fallback):** Selected if no STT key is configured. Pings `OFFLINE_STT_URL` (default `http://localhost:8000`). If unreachable, falls back to the local downloaded `whisper-cli.exe` binary.

### Intent Parsing & Language Models (LLM) Router
1. **Cloud Providers:** Routes to OpenAI (`gpt-4o-mini`), Google Gemini (`gemini-1.5-flash`), or Groq (`llama-3.3-70b-versatile`) if configured in `.env`.
2. **Local Ollama (Fallback):** Routes to local Ollama instance (`llama3.2` or user model) at `OLLAMA_BASE_URL` (`http://localhost:11434`).

### Text-to-Speech (TTS) Router
1. **Cloud Providers:** Routes to Fish Audio, ElevenLabs, or Azure Speech if corresponding API keys are set.
2. **Local Piper TTS:** Selected if local `piper.exe` binary and voice model are downloaded.
3. **Windows SAPI5 (Universal Fallback):** Executes native PowerShell `$sp.Speak()` commands using Windows built-in SAPI5 voices. Ensures pixi never breaks even without internet or cloud keys.

---

## 5. Parallel Problem & Pipeline Cancellation

Handling real-time voice interactions introduces significant race conditions:
* User speaks again while the assistant is mid-speech or processing.
* Rapid back-to-back voice activations cause overlapping TTS audio output.
* Long-running LLM inferences continue background writes after user cancels.

### pixi's Interruption Solution:

1. **Request Request ID Guard (`activeRequestId`):**
   ```ts
   let activeRequestId = 0;

   const cancelActivePipeline = () => {
     activeRequestId++;
     tts.stop(); // Immediately kills active SAPI5 / Piper processes
   };
   ```
2. **Atomic Step Verification:** Every async phase (STT, LLM, Action Execution, TTS) checks `if (currentReqId !== activeRequestId) return;`. If a user speaks or submits text, all previous pending pipeline executions abort immediately.
3. **Non-Blocking Background Tasks:** Fact extraction and rolling conversation summaries run detached in background promises using `.catch()` blocks so they never block real-time speech responses.

---

## 6. Onboarding Flow & First-Run Setup

pixi features an automated 3-step setup sequence managed by `setup-manager.ts` to ensure local offline models are ready:

```
[Application Startup]
        │
        ▼
Is Setup Complete? ──── Yes ───► Ready to interact
        │ No
        ▼
Auto-trigger 3-step background setup (`runFullSetupSequence`):
  ├── Step 1 (70%): Pull local Ollama model (`llama3.2`) via Ollama API
  ├── Step 2 (20%): Download `whisper-cli.exe` binary & `ggml-tiny.bin` model (~460MB)
  └── Step 3 (10%): Download `piper.exe` binary & `en_US-lessac-medium` voice (~60MB)
```

Progress is logged non-blockingly and can be queried via IPC handle `setup:status`.

---

## 7. After-Install User Flow

1. **First Launch:**
   - App launches into the Windows System Tray with a tray icon.
   - If launched without `--hidden`, the window pops up in Widget or Orb mode.
   - Automatically registers `openAtLogin` in Windows login settings.
2. **Global Hotkey:**
   - Pressing `Cmd/Ctrl + Shift + Space` anywhere toggles window visibility and focuses input.
3. **Voice Activity Detection (VAD):**
   - Offline local VAD monitors microphone volume spikes (warmup filtering + sustained vocal energy threshold).
   - Alternatively, continuous Web Speech API listens for wake words (`"Hey pixi"`, `"pixi"`).
   - Upon wake word detection, a wake chime plays, status updates to `🎙 Listening...`, and PCM audio recording starts.
   - 4 seconds of silence triggers automatic audio submission.
4. **Mode Switching:**
   - Click the mode switcher icon to toggle between **Floating Pixel Blob Mascot Orb** (100x100 transparent overlay) and **Catppuccin Windows 11 Widget Bar** (580x60 horizontal command bar).

---

## 8. Memory & Context System

pixi maintains long-term memory across sessions using a hybrid SQLite + Markdown strategy stored strictly inside `%APPDATA%\pixi\`:

### Local Filesystem Layout
- **SQLite Database (`%APPDATA%\pixi\assistant.db`):** Stores structured tables for `sessions`, `messages`, `reminders`, and `todos`.
- **Markdown Memory (`%APPDATA%\pixi\memory\`):**
  - `profile.md`: Core user bio, identity, and personal facts.
  - `preferences.md`: Communication preferences, themes, and habits.
  - `routines.md`: Recurring daily schedules and workflows.
  - `projects/`: Knowledge notes per user project.
  - `people/`: Important contacts and relationships.
- **Session Archives (`%APPDATA%\pixi\archive\sessions\*.jsonl.gz`):** Gzip-compressed archives of past chat trajectories.

### Per-Turn Context Pipeline
For every user message, pixi assembles a turn context:
1. Loads `profile.md`.
2. Performs keyword matching against `preferences.md`, `routines.md`, `projects/`, and `people/`.
3. Attaches the rolling summary of the active session.
4. Appends the last $N$ recent conversation turns from SQLite.

---

## 9. Dependencies & Tech Stack

| Layer | Dependency / Technology | Role |
| :--- | :--- | :--- |
| **Desktop App** | `electron` (^31.7.7) | Desktop container & Windows API integration |
| **UI Framework** | `react` (^18.3.1) + `tailwindcss` (^3.4.0) | Component layout & Catppuccin theme styling |
| **IPC Bridge** | `socket.io` & `socket.io-client` (^4.7.0) | High-speed IPC between Electron renderer and orchestrator |
| **Database** | `better-sqlite3` (^11.0.0) + `drizzle-orm` (^0.31.0) | Local high-performance relational storage |
| **Audio Capture** | `node-record-lpcm16` (^1.0.1) & Web Audio API | Microphone PCM stream capture |
| **Scheduling** | `node-cron` (^3.0.3) + `node-notifier` (^10.0.1) | Reminder triggers & native Windows notifications |
| **AI SDKs** | `@google/generative-ai`, `openai`, `groq-sdk`, `fish-audio` | Provider API clients |
| **Bundling & Build** | `tsup` (^8.0.0), `tsx`, `electron-builder` (^24.13.0) | TypeScript compilation and NSIS/Portable packaging |

---

## 10. Development & Build Commands

```bash
# Install dependencies
pnpm install

# Run database migrations
pnpm db:generate
pnpm db:migrate

# Start development server with live reload
pnpm dev
pnpm start

# Bundle for production and generate Windows executable installers
pnpm build
pnpm pack
```

Output installers are generated in `release/` (`pixi Setup X.Y.Z.exe` and `pixi-X.Y.Z-portable.exe`).
